# 경기장 현황 — 2026-09-26 (브랜치 `protkjj/arena-completion`)

작업지시서 "월요일 경기장 완성"(kj, 2026-09-25)에 대한 현황 보고서다. 목표는 결과가 아니라
**공정하고 재현 가능한 경기장**이다. 이 문서의 시뮬레이션 수치는 전부 **SMOKE** 또는 **PILOT**이다.
우위나 열위 결론은 내리지 않는다. 수치마다 재현 명령과 |ω|를 붙였다.

kj는 밤사이 자리를 비웠다. 그래서 새로 생긴 판단거리는 묻지 않고 6절 '결정 필요'에 모았다.
밤사이 생긴 사고와 조치(메모리 누수로 부푼 백그라운드 실행 종료·재개, 빠른 검증에서 M17 생략,
PILOT 스크립트 버그)는 부록 A.4에 경위를 적었다.

**2026-09-26 낮 갱신**: kj 결정(6·9·10·18 + 기울기 45°)을 8절에 반영했다. 로컬 커밋만 있고 push 전이다.
스모크 참조를 다시 만들기 전이라 verify 4단계가 설정 해시에서 멈추기 때문이다. 1절의 판정·수치는 push된
`d1c1cdb` 기준이다. 새 결정 필요는 8.7에 있다(M17 수정안, 기준선 참조 가속도 피드포워드).

---

## 1. 월요일 완성 기준 5개

| # | 기준 | 판정 | 근거 |
|---|---|---|---|
| 1 | 기체·저장소 고정 | **통과** | 기체 `light_rocket_v2_pack_forward`(1.17407572 kg, 파라미터 sha256 `13a1dcc6…`)와 저장소·기준 브랜치(`protkjj/fast-drone`, `control`)가 `configs/arena.json` `confirmed` 블록에 있다. I-7이 설정을 확정 사항과 대조하고, I-1이 5종 모두 같은 해시의 플랜트를 받는지 본다. |
| 2 | 모든 제어기 같은 조건 | **통과(남은 차이는 2.2절)** | 불변식 I-1~I-11. 플랜트·관측·미리보기(H = 1.0 s)·NMPC 설정·추진 모델·출발 정보·튜닝 예산이 같다. 해결한 기울기 15건과 남은 차이 11건(k는 2026-09-26 낮 추가)을 방향과 함께 2절에 적었다. |
| 3 | NMPC 원인 분류와 공통 정식화 | **부분 통과** | 작업 A가 콜드스타트 웜스타트 수치 실패를 세 NMPC에 똑같이 고쳤고, I-5가 세 종 모두 정확한 트림(0·20·85 m/s)에서 수렴함을 확인한다. 공통 정식화는 `control/nmpc_common.py`(IPOPT 옵션·비용 가중치·솔버 감시 한 곳)에 있다. 작업 A의 분류("H-시나리오")를 새 프로펠러 모델로 다시 판정한 결과는 3절이다. 85 m/s는 유지되고, 80 m/s는 기준에 따라 갈린다. |
| 4 | 설정 파일 하나·명령 하나 | **통과** | `configs/arena.json` 하나와 `python -m control.validation_suite --config configs/arena.json --smoke` 명령 하나로 5종 × 7사례 35시행을 모두 실행했다(약 1.5시간). 시행마다 끝까지 갔는지·어디서 멈췄는지는 5절이다. |
| 5 | 재현성 | **통과(같은 머신)** | `scripts/verify_arena.py`가 전체 11.8분·빠른 모드 1.7~2.1분에 ALL PASS다. 재실행 사례는 전부 참조와 궤적 해시가 비트 동일했다. 그 밖의 근거: I-6(같은 사례 두 번 → 비트 동일), 튜닝 재개(12→24)가 처음부터 돌린 것과 115개 시나리오 궤적 해시까지 동일, 서로 다른 커밋·부하의 두 스모크 실행이 겹치는 10행에서 10/10 비트 동일. 다른 머신 허용오차(rtol 1e-3)는 잠정이다(결정 필요 13). |

---

## 2. 공정성 현황표

방향 표기: **↓X** = X에게 불리했다(또는 불리하다), **↑X** = X에게 유리했다(또는 유리하다).

### 2.1 해결된 것

| # | 항목 | 수정 전 | 방향 | 수정 | 검증 |
|---|---|---|---|---|---|
| 1 | 스위트가 팀원의 옛 제어기(2026-09-09 스냅샷)만 알았다 | 현재 제어기 5종을 돌릴 수 없었다 | — | `ArenaFactory`가 V13·M17·F13·GSLQR·CPID를 등록. 옛 제어기는 결과에 안 섞는다 | I-1, I-2 |
| 2 | 호버 자세 규약이 팀 플랜트와 추력축 둘레 **180°** 달랐다 | GSLQR(트림 대비 오차)과 CPID(목표 방위)가 가짜 180° 롤 오차를 봤다 | ↓GSLQR ↓CPID | 제어기 파라미터에 `hover_quat`(팀 규약). 트림이 처음부터 플랜트 규약으로 나온다 | I-9(음성 대조군: 규약을 안 맞추면 2초 안에 90° 넘게 돈다), 규약 변환 교차검증 |
| 3 | 프로펠러 모델이 선형 `1−J/J_max`였다 | 트림 추력을 20 m/s 22%, 85 m/s 57% 과소평가. 그 모델의 트림은 로터 회전수를 20 m/s에서 9~24%, 85 m/s에서 9~10% 높게 잡았다. 85 m/s 회전수 여유는 모델에서 ~0%(실제 9%) | 전 제어기(모델 기반일수록 큼) | 팀원 APC PCHIP 곡선을 5종 + INDI 배분·T_meas 전부에(cb08eee 병합) | I-8(운용 범위 추력·토크 ±5%). 지금 명목 트림은 플랜트 트림과 로터별 0.51% 이내 |
| 4 | CPID 추력→회전수 역산이 정지 공식 `sqrt(f/k_T)`였다 | 35 m/s 이상에서 명령 회전수가 영추력점 아래로 떨어졌다 | ↓CPID | 현재 축방향 유속에서 곡선을 brentq로 역산, 반토크비 γ(J) 배분 | I-8(트림 추력 ±5%, 정지 공식이면 실패하는 판별 시험 포함) |
| 5 | F13 반토크비가 상수였다 | 곡선에서는 Q/T가 J에 따라 변한다(J 0.79에서 CT −12%, CP +44%) | ↓F13 | NLP 파라미터 γ(측정 회전수의 J) | I-8 |
| 6 | F13 어댑터가 공력 모멘트를 빠뜨렸다 | `ω̇_des = (M − ω×Jω)/J`에 M_aero 없음 → 85 m/s에서 \|ω\| 21.8 | ↓F13 | `(M + M_aero − ω×Jω)/J` | I-9(85 m/s \|ω\| 0.28로) |
| 7 | 압력중심 x_cp가 상수였다(논문 식 9) | 트림 모멘트 오차 최대 0.203 N·m | 모델 기반 4종(V13·M17·F13·GSLQR) | x_cp(V) 3차 스케줄. 오차 0.0034 N·m(≈0.62 rad/s²) | I-10(트림 각가속도 ≤ 2 rad/s²: 실측 ≤ 0.23. 명목 트림 = 플랜트 트림: 회전수 ≤ 0.51%, 자세 ≤ 0.64°) |
| 8 | NMPC 설정이 파일마다 따로였다(M17만 `max_iter` 인자·수렴 추적 없음) | 같은 값이라는 보장이 없었다 | 불명 | `nmpc_common`: IPOPT 옵션·비용 가중치 한 곳, `SolverMonitor` 한 구현 | I-3 |
| 9 | 웜스타트 규칙이 NMPC마다 달랐다 | — | 불명 | 세 NMPC 모두 X = 실측, U = 명목 모델 트림 입력 | I-3, I-5 |
| 10 | 참조 미리보기가 제어기마다 달랐다 | — | ↑NMPC | 같은 `ReferenceWindow` 객체, H = N·Δt = 1.0 s 초과 요청은 예외 | I-2 |
| 11 | CPID 수평 속도에 적분기가 없었다 | P만으로 순항 항력을 버텨 10·20 m/s에서 정상오차 1.39·3.4 m/s | ↓CPID | 조건부 속도 적분(kj 결정, 논문 §5.3 "적분기는 포화 시 조건부 적분") | 설계점검(4절) |
| 12 | CPID 적분 한계가 상태 크기로 정의돼 있었다(속도 5 m, 고도 5 m·s) | 적분이 낼 수 있는 가속도가 Ki에 묶였다. 속도 0.75 m/s²(10·20 m/s 시행의 59~86%를 한계에 붙어 있음), 고도 2.5 m/s²(20 m/s 트림 유지에 2.85 필요 → 고도오차 +0.175 m 영구) | ↓CPID | 두 적분기 모두 **가속도 권한 ±1 g**(PX4 원본과 같게), 포화 시 조건부. **설계점검 결과를 본 뒤의 수정이고, 튜닝 예산 밖이며, CPID에 유리한 방향이다**(kj 요구로 명기). 기본값은 기존 동작과 비트 동일, 경기장 설정에서만 켠다 | I-11, 4.2절 |
| 13 | CPID만 트림 정보 없이(적분기 0) 출발했다 | 공력 피드포워드가 없어 트림을 적분기로만 유지하는데, 20 m/s에서 채워지는 데 ~30초. 정확한 트림에서 출발해도 5초 뒤 고도 +0.52 m, 속도 −2.3 m/s | ↓CPID | 출발 때 명목 모델 트림값으로 적분기를 채운다(NMPC 웜스타트와 같은 정보). 12행과 같은 kj 결정·같은 기록 조건 | I-11, 설계점검 3/6 → 6/6 |
| 14 | 옛 "동일 예산" 튜닝이 실제로는 GSLQR 360회, CPID 240회였다(research) | 기준선이 더 많이 평가받았다 | ↑GSLQR ↑CPID | 첫 평가부터 예산에 넣고, 캐시 적중도 똑같이 계상하고, 예산을 정확히 다 쓴다. 무료 사전선별 없음 | I-4(합성 기록 검사 + 커밋된 기록 검사, 5절 튜닝 요약) |
| 15 | 튜닝과 본시험 시나리오·난수 분리가 코드로 강제되지 않았다 | — | 튜닝 과적합 | 설정 검증이 ID 교집합을 막고, 난수 범위가 서로소(본시험 1000~1999, 튜닝 2000~2999) | I-4, `validate_config` |

### 2.2 아직 남은 차이(방향과 처리)

| # | 항목 | 방향 | 크기·근거 | 처리 |
|---|---|---|---|---|
| a | V13·F13 NLP 추력 상한은 정적(논문 식 18: 영 유속 최대 회전수의 총추력). M17은 로터 회전수를 직접 제한해 J에 따른 가용 추력을 모델로 안다 | ↓V13·F13(고속에서 낼 수 없는 추력을 계획할 수 있다) | V13·F13 둘은 대칭이다(kj 요청으로 확인). F13에 넣은 측정 γ(J)는 로터 추력→스핀축 반토크 환산만 바꾸고 추력 상한은 안 바꾼다(I-3이 두 상한을 묶어 둔다). V13은 NLP 입력이 가상입력(T, α)이라 같은 환산이 NLP 밖, INDI 배분의 곡선 dQ/dn에 있다 — 정보는 같고 위치만 다르다. 짧은 시험에서 V13 포화 90~97%(3절)가 이것 때문인지는 영향 확인 필요 | 논문 식 그대로라 유지. 결정 필요 16 |
| b | GSLQR LQI 적분 한계는 상태 크기(ξ ±5)라 CPID(가속도 권한)와 기준이 다르다 | 약하게 ↑GSLQR | 가속도 환산 권한 x 10.6~20.9, z 13.6~70.6 m/s²(CPID 9.81). 설계점검 10/10에서 한 번도 안 닿았다(한계 ∞와 비트 동일). 24회 튜닝 115개 시나리오 평가에서도 한계 도달 0%. **그러나 스모크 섭동 임무에서 ξ_vx가 10.97%(질량 ×1.3)·4.89%(모터 τ ×2) 한계에 닿아 1% 규칙에 걸렸다** | kj 결정대로 유지·계측했고, 1% 규칙에 따라 결정 필요 9로 올렸다. **→ 8.2: 한계 50으로 넓힘(전 스모크 사례 0%)** |
| c | CPID는 설계 영역(0~20 m/s) 밖에서 구조적으로 못 난다 | ↓CPID(V_H 사례) | 기울기 한계가 세계 수직 기준 35°인데 85 m/s 트림 추력축은 수직에서 ~83°다. 적분기를 트림값으로 채워도 ±1 g에서 잘린다 | 작업지시서가 CPID는 호버·저속만 요구한다. V_H 사례의 CPID 실패는 '설계 영역 밖'이라는 뜻이지 성능 비교가 아니다. 결정 필요 6. **→ 8.4: V_H 본시험에서 제외, V_L 돌풍 추가, 기울기 45°** |
| d | 튜닝 목적함수의 실패 벌점이 고정값(1000)이라 CPID 튜닝 신호가 V10 시나리오 하나에서만 나온다 | ↓CPID | 튜닝 시나리오 5개 중 CPID 설계 영역 안은 1개(V10 측풍). 24회 파이프라인에서 CPID 목적함수 800.019 → 800.011 = 설계 영역 밖 4개가 매번 실패(벌점 고정) + V10 한 개. 실패 92건 = 23회 × 4 | kj 지시로 밤중에는 목적함수를 안 바꿨다. 선택지는 결정 필요 10. **→ 8.3: 공통 집합에 저속 3개 추가** |
| e | CPID 구현이 논문 §5.3 서술과 세부가 다르다 | 혼재 | (1) 자세 PD 한 단(논문: 자세 루프 + 각속도 루프, PX4는 각속도 적분항 있음) → 일정 모멘트 외란을 자세 오프셋으로 버틴다 ↓CPID. (2) 미분에 저역통과 필터 없음(잡음 없는 시뮬레이션에선 지연이 없어) ↑CPID. (3) 배분이 회전수 클리핑(논문: 정적 제약 배분) ↓CPID(포화 시) | 영향 확인 필요. 결정 필요 7 |
| f | 제어기 모델 ≠ 플랜트(집중정수 적합) | 모델 기반 제어기에 ↓ | 피치 모멘트 적합 R² 0.775. CPID는 모델을 거의 안 쓴다(추진 곡선·출발 트림만) | 작업지시서 확정 사항(명목부터 플랜트 ≠ 제어기). 유지 |
| g | NMPC 솔브 시간이 다르다(V13 95 ms, F13 130 ms, M17 210~320 ms, 논문모드) | ↑M17(실시간 제한이 없어서) | 원인 분석 모드라 시간 제한을 걸지 않았다(결과가 벽시계에 의존하면 안 된다는 kj 조건) | 계산 비교 모드에서 따로 공개 |
| h | 추진 모델 도메인 판정이 매우 엄격하다(10k rpm 하한, 역유입 1e-8) | 전 제어기 | 거의 모든 시행이 domain 실패로 찍힌다 | tracking과 domain을 분리해 보고 |
| i | 가중치·게인이 아직 이 기체에서 제대로 튜닝되지 않았다 | 혼재 | NMPC는 논문 리터럴, GSLQR은 옛 기체 튜닝값, CPID는 클래스 기본값. 24회 파이프라인은 폴링 두 번 분량이라 튜닝으로 볼 수 없다 | 120회 본 튜닝(kj 지시로 시작하지 않음, 부록 A에 명령) |
| j | V_H 외란·제동에서 M17만 IPOPT 반복 상한(30)에 연속으로 걸린다(스모크 5절) | ↓M17(원인에 따라 경기장 탓일 수 있음) | 평소 반복 중앙값 5회. 실패 직전 8~12회 → 30회로 뛴다(수직풍은 29회까지 올랐다가). 같은 사례에서 V13·F13은 최대 5회. 원인 후보: (H1) 85 m/s 회전수 여유 9%라 M17 NLP의 회전수 제약이 활성화된다(V13·F13은 정적 추력 상한이라 비활성 — 2.2-a의 반대 면). (H2) NLP 안에 C1 곡선(PCHIP)을 품은 것은 M17뿐이라 2차 미분 불연속이 뉴턴 수렴을 방해할 수 있다. H2면 경기장의 모델 표현이 M17에 불리한 것이다. kj가 곡선 도입 때 걱정한 '미분 연속성이 IPOPT 수렴을 해치는가'는 정확한 트림(I-5)에서는 문제가 없었지만, 외란 아래에서는 이 관찰 때문에 다시 열린다 | 영향 확인 필요. 결정 필요 18. **→ 8.1: 원인은 추력 0 평탄 구간(H4), 수정은 kj 결정 대기** |
| k | 기준선(CPID·GSLQR)은 참조 가속도·미리보기를 쓰지 않는다. 매 스텝 현재 참조만 받는다(`arena_factory.update_at`). 정보는 같지만(2.1의 10행) 구조가 쓰지 않는다. PX4 `PositionControl`은 궤적 가속도를 피드포워드로 더한다 | ↓CPID·GSLQR(참조가 변하는 사례에서만) | 튜닝 가속 0→15에서 창 RMSE v CPID 2.72·GSLQR 0.58, NMPC 0.035~0.072(8.3). 1차 어림으로 CPID 몫의 대부분이 피드포워드 부재다. 본시험의 CPID 사례(V_L 돌풍)는 참조가 일정해 영향이 없다. GSLQR은 V_H 참조 변화 사례(제동·가감속)에서 영향이 있을 수 있다(영향 확인 필요) | 2026-09-26 낮에 발견. 새 결정 필요(8.7) |

---

## 3. 작업 A 판별 결과

**작업 A 세션의 판정**(`results/SOLVER_FAILURE_REPORT_2026-09-25.md` §6·§7):
- 콜드스타트 웜스타트가 진짜 수치 실패였다. V13·M17·F13에 똑같이 고쳤다.
- 짧은 시험(80·85 m/s 트림 출발 4초, 초기오차 +0.2 m·+0.5 m/s)은 고친 뒤에도 실패했고, 65초 미션은 같은 속도를 지나가며 완주했다.
- 판별 실험에서 트림 웜스타트와 쿼터니언 정칙화를 모두 줘도 짧은 시험이 똑같이 실패했다. 그래서 **H-시나리오 지지, H-웜스타트이력(단발 형태) 기각**으로 판정했다.

**다시 판정하는 이유**: 그 판정은 선형 프로펠러 모델로 얻었다. 그때 V13의 포화율 91%가 모델 결함
(트림 추력 57% 과소평가) 탓이라면 전제(짧은 시험 실패)부터 달라진다.

**다시 돌린 결과**(PILOT, `python -m control.pilot_before_after --parts discriminate`, 현재 모델 = 곡선 + x_cp(V)):

| 사례 | 시점 | 완주 | 시간 s | z RMSE | v RMSE | \|ω\|max | 포화 % | 솔버실패 | 정지사유 |
|---|---|---|---:|---:|---:|---:|---:|---:|---|
| 80 기본 | 이전(선형) | ✗ | — | 0.697 | 13.12 | — | 91.4 | 2/50 | velocity error limit |
| 80 기본 | 현재 | ✓ | 4.00 | 2.371 | 11.43 | 18.9 | 95.8 | 2/200 | — |
| 80 트림 웜스타트 | 이전(선형) | ✗ | — | 0.698 | 13.12 | — | 91.2 | 2/50 | velocity error limit |
| 80 트림 웜스타트 | 현재 | ✓ | 4.00 | 2.310 | 11.43 | 18.9 | 96.6 | 2/200 | — |
| 85 기본 | 이전(선형) | ✗ | — | 0.695 | 14.35 | — | 91.2 | 3/51 | velocity error limit |
| 85 기본 | 현재 | ✗ | 2.11 | 1.389 | 13.81 | 21.1 | 92.9 | 2/106 | velocity error limit |
| 85 트림 웜스타트 | 이전(선형) | ✗ | — | 0.698 | 14.36 | — | 91.5 | 2/51 | velocity error limit |
| 85 트림 웜스타트 | 현재 | ✗ | 1.88 | 1.447 | 14.00 | 19.2 | 89.4 | 2/95 | velocity error limit |

**판정 규칙을 다시 적용하면**:
- **85 m/s**: 기본·트림 웜스타트 둘 다 실패한다. **H-시나리오 지지가 유지된다.**
- **80 m/s**: 기준에 따라 갈린다.
  - 작업 A 표의 기준(완주)으로는 둘 다 4초를 완주해 판별의 전제가 사라진다.
  - 추종 기준(끝 오차와 포화 < 1%)으로는 둘 다 실패한다(포화 96~97%, v RMSE 11.4).
- **두 속도 모두 기본과 트림 웜스타트의 결과가 거의 같다.** 그래서 H-웜스타트이력(단발 형태) 기각은 모델을 바꿔도 그대로다.
- '프로펠러만'(x_cp 상수) 변형은 현재 모델과 모든 수치가 같다. V13 예측 모델에 압력중심이 들어가지 않기 때문이고, 바뀐 몫은 전부 프로펠러 곡선이다.
- 주의: 80 m/s RMSE는 전후를 직접 비교하지 말 것. 이전은 ~1초에 멈췄고 지금은 4초를 다 채워서 평균 구간이 다르다.

**해석(가설, 영향 확인 필요)**: 올바른 추진 모델에서도 V13이 짧은 시험에서 90% 넘게 포화한다.
후보는 2.2-a다. NLP 추력 상한이 영 유속 기준(식 18)이라, 80~85 m/s에서 로터가 실제로 낼 수 없는
추력을 계획할 수 있다. 이를 확인하려면 계획 추력과 가용 추력(n_max, 현재 J)을 대조해야 한다(다음 세션 후보).

**65초 미션·가감속 설정 전후**(PILOT, V13, `python -m control.pilot_before_after`, 결과 `results/arena/PILOT_BEFORE_AFTER.md`):
전후 사이에 바뀐 것은 프로펠러 곡선과 x_cp(V)다.
기록된 git은 `cb1841c`(dirty=True)다. dirty 원인은 결과와 무관한 `scripts/verify_arena.py` 미커밋 수정이다.

| 사례 | 시점 | 완주 | 시간 s | z RMSE | v RMSE | \|ω\|max | 포화 % | 솔버실패 | 정지사유 |
|---|---|---|---:|---:|---:|---:|---:|---:|---|
| 미션 70 | 이전 | ✓ | 65 | 0.524 | 2.869 | 11.2 | 6.5 | 2/3250 | |
| 미션 70 | 이후 | ✓ | 65 | 0.285 | 2.749 | 10.6 | 5.8 | 2/3250 | |
| 미션 80 | 이전 | ✓ | 65 | 1.307 | 3.538 | 16.2 | 7.5 | 2/3250 | |
| 미션 80 | 이후 | ✓ | 65 | 0.628 | 3.608 | 13.3 | 7.5 | 2/3250 | |
| 미션 85 | 이전 | ✓ | 65 | 0.833 | 3.267 | 12.6 | 8.1 | 2/3250 | |
| 미션 85 | 이후 | ✓ | 65 | 0.269 | 4.017 | 13.9 | 7.1 | 2/3250 | |
| 원래 15/15/15 @70 | 이전 → 이후 | ✓ → ✓ | 55 | 0.360 → 0.265 | 1.852 → 1.997 | 10.0 → 10.5 | 7.7 → 6.6 | 2 → 1 /2750 | |
| 원래 @80 | 이전 → 이후 | ✓ → **✗** | 55 → 40.98 | 0.217 → 1.554 | 1.771 → 3.484 | 16.0 → 15.3 | 8.3 → 10.2 | 4/2750 → 7/2050 | → 5연속 최적화 실패(감속 7.98 s째) |
| 원래 @85 | 이전 → 이후 | ✓ → ✓ | 55 | 2.374 → 0.255 | 4.370 → 3.758 | 14.6 → 13.8 | 14.6 → 8.7 | 15 → 2 /2750 | |
| 완화 8/3/25 @70 | 이전 → 이후 | ✓ → ✓ | 40 | 0.136 → 0.183 | 1.275 → 1.126 | 12.1 → 13.4 | 8.6 → 6.1 | 2 → 2 /2000 | |
| 완화 @80 | 이전 → 이후 | ✓ → ✓ | 40 | 0.414 → 0.234 | 1.841 → 1.839 | 15.4 → 10.3 | 12.7 → 11.0 | 5 → 3 /2000 | |
| 완화 @85 | 이전 → 이후 | ✓ → ✓ | 40 | 0.642 → 0.404 | 3.198 → 1.986 | 14.7 → 15.0 | 13.8 → 12.2 | 3 → 2 /2000 | |

- 완주 여부가 바뀐 사례는 **원래 설정 80 m/s 하나**다(완주 → 감속 중 40.98 s에 솔버 연속 5회 실패로 정지).
  원인은 조사하지 않았다(다음 세션 후보). 참고로 논문 §5.9도 "예비 시험에서 V13은 감속 구간에서 성능이
  저하"라고 적고 있다. 스모크의 M17 통합임무도 감속 초입에서 솔버 실패로 멈췄다(5절).
- 가감속 실행 중 CasADi가 V13 NLP 제약식에서 NaN 경고를 한 번 냈다(`vnmpc_paper:nlp_g ... row 221`).
  첫 사례(원래 @70, 완주, 솔버 실패 1/2750) 도중이었다. 작업 A가 쿼터니언 정규화를 eps로 고친 뒤에도 NaN
  경로가 하나 남아 있다는 뜻이라 원인 확인이 필요하다.
- 이 표로 설정 우열을 말하지 않는다. 작업 A 보고서 §2.4의 "원래(15/15/15)를 기본, 완화를 민감도용"
  권고는 kj 결정 사항으로 남아 있다.

---

## 4. GSLQR·CPID 설계 영역 확인

### 4.1 계단 응답(PILOT)

재현: `python -m control.arena_design_check` · 커밋 `56458e8` · 결과 `results/arena/DESIGN_CHECK.md`.
팀 플랜트 명목, 정확한 트림에서 출발해 t = 0.5 s에 계단, 15 s 관찰. 정착 대역은 계단의 ±5%.
설계 영역의 윗끝(GSLQR 85, CPID 20)에서는 속도 계단을 −1 m/s로 준다(+1이면 스케줄 표 밖이라 참조가 잘린다).

| 제어기 | 속도 m/s | 계단 | 정착 s | 오버슈트 % | 정상오차 | 교차결합 | \|ω\|max | 적분 한계 % | 적분 정지 % |
|---|---:|---|---:|---:|---:|---:|---:|---:|---:|
| GSLQR | 0 | 고도 +1 m | 3.54 | 28.1 | 3.4e-05 | 2.1e-12 | 6.0e-12 | 0 | 0 |
| GSLQR | 0 | 속도 +1 m/s | 2.34 | 26.9 | 1.4e-05 | 0.027 | 1.93 | 0 | 0 |
| GSLQR | 20 | 고도 +1 m | 3.74 | 30.0 | 4.1e-05 | 0.233 | 1.56 | 0 | 0 |
| GSLQR | 20 | 속도 +1 m/s | 2.70 | 12.1 | 7.3e-06 | 0.022 | 1.44 | 0 | 0 |
| GSLQR | 40 | 고도 +1 m | 2.79 | 33.4 | 2.3e-05 | 0.439 | 3.67 | 0 | 0 |
| GSLQR | 40 | 속도 +1 m/s | 2.41 | 9.8 | 1.4e-05 | 0.017 | 0.46 | 0 | 0 |
| GSLQR | 60 | 고도 +1 m | 2.60 | 31.8 | 1.7e-05 | 0.157 | 3.47 | 0 | 0 |
| GSLQR | 60 | 속도 +1 m/s | 1.83 | 7.9 | 1.1e-05 | 0.016 | 0.20 | 0 | 0 |
| GSLQR | 85 | 고도 +1 m | 2.46 | 27.9 | 1.5e-05 | 0.067 | 3.37 | 0 | 0.03 |
| GSLQR | 85 | 속도 −1 m/s | 1.47 | 7.2 | 9.1e-06 | 0.008 | 0.12 | 0 | 0 |
| CPID | 0 | 고도 +1 m | 6.89 | 26.9 | 0.0044 | 6.1e-12 | 7.8e-12 | 0 | 0 |
| CPID | 0 | 속도 +1 m/s | 8.72 | 8.7 | 0.020 | 0.0005 | 1.06 | 0 | 0 |
| CPID | 10 | 고도 +1 m | 7.04 | 29.1 | 0.0049 | 0.045 | 0.26 | 0 | 0 |
| CPID | 10 | 속도 +1 m/s | 11.54 | 0.0 | 0.037 | 0.073 | 1.03 | 0 | 0 |
| CPID | 20 | 고도 +1 m | 7.24 | 31.3 | 0.0048 | 0.105 | 1.09 | 0 | 0 |
| CPID | 20 | 속도 −1 m/s | 8.39 | 0.0 | 0.024 | 0.112 | 1.06 | 0 | 0 |

게인은 전부 사전값이다(GSLQR은 옛 기체 튜닝값, CPID는 클래스 기본값 + Ki_vel 0.15).

**튜닝 뒤 점검**(kj 지시: 같은 예산 튜닝 뒤에도 CPID가 0~20 m/s에서 수렴하지 못하면 결정 필요):
120회 본 튜닝은 kj 지시로 돌리지 않았다. 그래서 24회 파이프라인의 최선값(GSLQR q_v_h ×4, CPID Kp_vel ×4)으로
같은 점검을 돌렸다. 재현은 `python -m control.arena_design_check --tuned results/arena/tuning/pilot24`,
결과는 `results/arena/design_check_pilot24/DESIGN_CHECK.md`(커밋 `931897d`)다.
- **CPID 6/6 정착**. 0 m/s 속도 0.51 s, 10 m/s 고도 7.02 s·속도 11.74 s, 20 m/s 고도 7.49 s·속도 0.37 s.
  \|ω\|max는 최대 4.68로 커졌다(Kp_vel ×4). 적분 한계 도달 0%.
- **GSLQR 10/10 정착**. 속도 계단 정착이 0.22~1.91 s로 빨라졌다.

120회 뒤에 같은 명령으로 다시 확인해야 한다.

**CPID 수정 경과**(같은 점검, 재현: `python -m control.arena_integrators --history`, 결과 `results/arena/INTEGRATORS.md`):

| 단계 | 10 m/s 속도 계단 | 20 m/s 고도 계단 | 20 m/s 속도 계단 | 한계 도달 비율 |
|---|---|---|---|---|
| P만(적분 없음) | 미정착, 정상오차 1.39 m/s | 정착 10.63 s(오버슈트 91.7%) | 미정착, 정상오차 3.4 | — |
| + 속도 적분, 상태 한계 5 m(= 0.75 m/s²) | 미정착, 0.815 | 정착 10.91 s(오버슈트 99.5%) | 미정착, 2.84 | 0.59~0.86 |
| + 한계를 가속도 권한 5 m/s²로 | 미정착, 0.257 | 미정착, 오버슈트 101.6% | 미정착, 오버슈트 297% | 0 |
| 현재: ±1 g 두 적분기·조건부·출발값 | **11.54 s 정착** | **7.24 s 정착** | **8.39 s 정착** | 0 |

앞의 두 단계에서 20 m/s 고도 계단이 '정착'한 것은 개선이 아니다. 속도 적분이 없거나 막혀서 비행기가
~16 m/s로 느리게 날았기 때문이다. 속도가 낮으면 양력 몫이 작아져 고도 적분 부담이 준다.
셋째 단계에서 넷째 단계로 넘어가며 바뀐 것은 세 가지다: 고도 적분 한계, 조건부 적분, 출발값 사전 채움.
이 중 앞의 둘은 넷째 단계 설계점검에서 효과가 없다. 한계를 ∞로 바꿔도 6/6 비트 동일했고(`--binding`),
적분 정지 스텝은 0%였다. 그러니 셋째 단계의 미정착은 **적분기 냉시동** 탓이다.

### 4.2 CPID 적분 한계 수정 — 구조적 수정인가, 성능 보정인가(kj 요청)

**구조적 수정(정의 오류)이다. 한계값을 조정한 성능 보정이 아니다.** 근거는 다음과 같다.

1. **정의가 기능을 막았다.** 처음 구현은 적분 상태(∫e, m)를 5로 잘랐다. 그러면 적분이 낼 수 있는
   가속도가 Ki × 5 = 0.75 m/s²(Ki 0.15)다. 이 적분기가 지워야 하는 트림 항력은 10 m/s 1.47,
   20 m/s 4.64 m/s²다(명목 모델 트림). 한계가 기능보다 작아서 구조상 정상오차를 못 지운다.
   실측으로도 10·20 m/s 시행의 59~86%를 한계에 붙어 있었다.
2. **권한이 튜닝 게인에 묶였다.** 상태 단위 한계면 튜닝이 Ki를 바꿀 때 적분 권한도 같이 바뀐다.
   가속도 단위면 Ki와 무관하다(I-11이 Ki를 4배·¼배로 바꿔도 한계가 그대로인지 본다).
3. **기준 구조와 같게 했다.** 논문이 CPID를 대응시키는 PX4의 `PositionControl::_velocityControl`
   (main 브랜치 원본 확인)은 `_vel_int += vel_error.emult(_gain_vel_i) * dt`로 Ki를 먼저 곱해
   가속도 단위로 쌓고, 수직 적분을 게인과 무관하게 ±`CONSTANTS_ONE_G`로 자른다. 한계값 1 g도
   kj 결정으로 PX4와 같게 했다.
4. **값이 결과를 만들지 않았다.** 현재 설정(±1 g)에서 한계를 ∞로 바꿔도 설계점검 궤적 해시가
   CPID 6/6, GSLQR 10/10 비트 동일하다(`python -m control.arena_integrators --binding`).
   설계점검·24회 튜닝 모두 한계 도달 0%다.

**단서**:
- 수정 시점이 설계점검 결과를 본 뒤였다.
- 튜닝 예산 밖이었다.
- CPID에 유리한 방향이다(기준선 강화 쪽이라 절대규칙 2와는 맞다).

이 세 가지를 공정성 현황표 12·13행에 적었다.

고도 적분(기존 코드)도 같은 종류의 문제였다.
- 상태 단위 한계라 권한이 2.5 m/s²였고, 무조건 적분이라 논문 §5.3과도 달랐다.
- kj 결정으로 속도 적분과 같은 기준(±1 g, 조건부)으로 맞췄다.
- 20 m/s 60초 유지 시험(`--hold`)에서 기존 방식은 한계에 100% 붙은 채 고도오차 +0.175 m가 남았다. 현재 방식은 오차 0.0000 m, 한계 도달 0%다.

### 4.3 GSLQR LQI 적분기의 한계 기준(kj 요청)

**같은 기준이 아니다.** GSLQR은 적분 **상태** ξ(고도 m·s, 전진속도 m)를 ±5로 자른다. CPID의 수정 전 방식과 같은 종류다.
포화 시 조건부 적분(로터 명령이 하나라도 포화하면 정지)은 이미 되어 있다.

| 항목 | 값 | 재현 |
|---|---|---|
| 가속도 환산 권한(LQI 정상상태 해석: 단위 외란 가속도당 필요한 ξ로 한계 5를 나눔) | x 10.6~20.9, z 13.6~70.6 m/s²(0~85 m/s, 사전 가중치) | `python -m control.arena_integrators` |
| 증강 폐루프 최대 실수부 | −0.689 ~ −0.708(전 운용점 안정) | 같음 |
| 설계점검에서 한계에 닿은 스텝 | 0(10건 전부). ξ 최대 0.53/5 | `python -m control.arena_design_check` |
| 한계 5 vs ∞ | 궤적 해시 10/10 비트 동일 | `python -m control.arena_integrators --binding` |
| 24회 튜닝 115개 시나리오 평가 | 한계 도달 0% | `python -m control.arena_tune --summarize --run-dir results/arena/tuning/pilot24` |
| **스모크 섭동 임무** | **ξ_vx 한계 도달 10.97%(질량 ×1.3)·4.89%(모터 τ ×2) — 1% 규칙 걸림**. 명목 통합임무 0.5% | `results/arena/SMOKE_2026-09-26.md` |
| 질량 +30% 정상상태 필요량(z) | 2.94 m/s² — 85 m/s 권한 70.6의 4% | 해석 |

kj 결정: 기준은 그대로 두고, CPID와 **같은 형식**으로 한계 도달·적분 정지 스텝을 모든 시행에 기록한다.
튜닝·스모크 중 어느 제어기든 한계 도달이 시행의 1%를 넘으면 결정 필요로 올린다.
구현은 `arena_factory.IntegratorLog`, 스위트 행의 `integrators` 필드, `arena.integrator_limit_flags`다.
권한이 튜닝 가중치(q_iz, q_ivx, r)에 따라 변하는 점은 남아 있다. 그래서 계측으로 본다.

---

## 5. 스모크 결과(SMOKE — 결론 없이 수치만)

재현: `python -m control.validation_suite --config configs/arena.json --smoke --reference-out results/arena/smoke_reference.json`
(약 1.5시간, 결과 표 전체 `results/arena/SMOKE_2026-09-26.md`, 참조 `results/arena/smoke_reference.json` 35행)
실행 git은 `871ca66`(dirty: 결과와 무관한 `scripts/verify_arena.py`만 미커밋)이고, 환경은 Apple M4·Python 3.13.7·스레드 1이다.
같은 10행을 깨끗한 커밋 `931897d`에서 따로 돌린 결과와 궤적 해시가 **10/10 비트 동일**했다.

표의 '끝까지'는 시행을 정지 없이 마쳤다는 뜻이고, 판정은 아니다. 판정(스위트·논문 §5.10)은 요약 문서에 있다.
괄호 안은 정지 시각과 사유다(솔버 = 연속 5회 최적화 실패, 발산 = |ω| 35 rad/s 또는 고도 50 m 이탈).

| 사례 | V13 | M17 | F13 | GSLQR | CPID |
|---|---|---|---|---|---|
| 통합임무 V_H(55 s) | 끝까지, \|ω\| 13.7 | 36.3 s 솔버(감속 초입), \|ω\| 0.21 | 끝까지, \|ω\| 9.8 | 44.0 s 발산(감속), \|ω\| 35.2 | 11.4 s 발산 |
| 측풍 10 m/s @V_H(12 s) | 끝까지, \|ω\| 0.32 | 3.8 s 솔버, \|ω\| 0.19 | 끝까지, \|ω\| 0.25 | 끝까지, \|ω\| 0.63 | 1.4 s 발산 |
| 수직풍 −5 m/s @V_H(12 s) | 끝까지, \|ω\| 0.32 | 4.1 s 솔버, \|ω\| 0.11 | 끝까지, \|ω\| 0.25 | 끝까지, \|ω\| 0.15 | 1.4 s 발산 |
| 가속 0→V_H, ρ=1(11.5 s) | 끝까지, \|ω\| 1.78 | 끝까지, \|ω\| 1.78 | 끝까지, \|ω\| 1.78 | 6.3 s 발산, \|ω\| 35.7 | 6.3 s 발산 |
| 제동 V_H→V_L, ρ=1(52.1 s) | 끝까지, \|ω\| 0.32 | 24.2 s 솔버, \|ω\| 0.23 | 끝까지, \|ω\| 0.25 | 끝까지, \|ω\| 0.09 | 1.4 s 발산 |
| 통합임무 질량 ×1.3 | 끝까지, \|ω\| 14.2 | 35.1 s 솔버 | 39.9 s 솔버, \|ω\| 10.0 | 44.3 s 발산, \|ω\| 26 | 12.3 s 발산 |
| 통합임무 모터 τ ×2 | 32.9 s 발산, \|ω\| 35 | 36.3 s 솔버 | 21.1 s 솔버, \|ω\| 16.1 | 45.8 s 발산, \|ω\| 33.1 | 11.4 s 발산 |

관찰(결론 아님):
- **M17**은 V_H 외란·제동·통합임무 감속에서 IPOPT 반복 상한(30)에 연속으로 걸려 멈췄다. 비행 자체는 조용했다(|ω| ≤ 0.43). 원인 가설과 방향은 2.2-j다.
- **CPID**는 V_H에서 출발하는 모든 사례와 V_H까지 가속하는 사례에서 발산했다. 설계 영역(0~20) 밖이다(2.2-c). 85 m/s 트림은 수평 10.5 m/s²가 필요해 ±1 g 적분 권한을 넘고, 추력축도 수직에서 82.4° 기울어야 해서 기울기 한계 35°를 넘는다.
- **GSLQR**은 가속(ρ=1)과 통합임무 감속에서 |ω| 35로 발산했다. 게인은 옛 기체 튜닝값이다(2.2-i).
- 모터 시정수 ×2 임무는 **5종 모두** 도중에 멈췄다. 질량 ×1.3 임무는 V13만 끝까지 갔다.
- 질량 ×1.3의 V_H 트림은 존재한다. 플랜트 트림 회전수 여유가 8.99% → 8.44%다. 85 m/s에서는 무게 대부분을 동체 양력이 받친다(결정 필요 5에 대한 답).
- 추진 모델 도메인(2.2-h) 때문에 끝까지 간 시행도 스위트 판정이 대부분 실패다. 추종·도메인을 분리한 판정은 요약 문서에 있다.
- **|ω|max 읽는 법**: V_H에서 출발하는 세 사례(측풍·수직풍·제동)에서 V13·F13의 |ω|max가 자릿수까지 같다(0.3237…, 0.2518…). 확인해 보니 셋 다 **출발 0.15~0.16 s의 과도**에서 나온다. 명목 모델 트림과 플랜트 트림의 작은 차이를 첫 제어가 바로잡는 순간이고, 세 사례는 3 s까지 조건이 같다. 외란·제동이 시작된 3 s 이후의 최대는 V13 0.18/0.10/0.07, F13 0.17/0.10/0.07이다. GSLQR은 최대가 돌풍 중(4.7~4.8 s)에 나온다. 표의 |ω|를 외란 응답으로 읽지 말 것.

**적분기 1% 규칙(kj)** — 한계 도달이 시행의 1%를 넘은 기록 5건. 결정 필요 9에 올린다.

| 제어기 | 사례 | 한계 도달 | 채널 |
|---|---|---:|---|
| CPID | 측풍 @V_H | 100% | vx(출발값 10.5 m/s²가 ±1 g에서 잘림 — 설계 영역 밖) |
| CPID | 수직풍 @V_H | 100% | vx(같은 이유) |
| CPID | 제동 V_H→V_L | 100% | vx(같은 이유) |
| **GSLQR** | **통합임무 질량 ×1.3** | **10.97%** | **ξ_vx(상태 한계 ±5 m)** |
| **GSLQR** | **통합임무 모터 τ ×2** | **4.89%** | **ξ_vx** |

GSLQR의 적분 한계는 설계점검·24회 튜닝·명목 통합임무(0.5%)에서는 거의 안 닿았다. 섭동 임무의 가감속
구간에서는 전진속도 적분이 상태 한계 ±5 m에 닿는다. 한계가 이 시행 결과에 얼마나 영향을 줬는지(한계 ∞ 대조)는
확인하지 않았다. 4.3절의 "같은 기준인가" 질문이 여기서 실제 영향 문제로 바뀐다.

### 5.1 24회 튜닝 파이프라인(PILOT)

kj 지시에 따라 파이프라인 검증 목적이다. 제어기마다 같은 예산 24회, 같은 목적함수, 튜닝 전용 시나리오 5개로 돌렸다.
나침반 탐색이 24회로는 폴링 두 번 분량이라 좌표가 하나씩만 움직였다. **튜닝 결과로 읽지 말 것.**
M17의 '200.145 → 0.169'는 개선폭이 아니다. 사전값이 V60 측풍 튜닝 시나리오에서 솔버 연속 실패로 벌점(1000/5)을
받은 것이고, 최선값은 그 실패에서 벗어난 것이다(스모크의 M17 측풍 솔버 실패와 같은 양상, 2.2-j).
제어기 사이의 개선폭 비교에 쓰면 안 된다.
재현 명령은 부록 A.1, 기록은 `results/arena/tuning/pilot24/`, 요약은 `python -m control.arena_tune --summarize --run-dir results/arena/tuning/pilot24`다.

| 제어기 | 평가 | 목적함수(사전값 → 최선) | 움직인 좌표(최선/사전) | 실패한 시나리오 평가 | \|ω\|max(전 평가) | 적분 한계 도달 |
|---|---:|---|---|---:|---:|---|
| V13 | 24/24 | 0.1449 → 0.1135 | w_ω ×¼ | 0 | 0.71 | — |
| M17 | 24/24 | 200.145 → 0.1689 | w_ω ×¼ | 8(전부 V60 측풍, 솔버 연속 5회 실패) | 0.70 | — |
| F13 | 24/24 | 0.1833 → 0.1444 | w_v ×2, w_ω ×½ | 0 | 0.70 | — |
| GSLQR | 24/24 | 0.7319 → 0.4976 | q_v_h ×4 | 0 | 0.91 | 0%(115평가) |
| CPID | 24/24 | 800.019 → 800.011 | Kp_vel ×4 | 92(= 23회 × 설계 영역 밖 4개) | 12.7 | 0%(115평가) |

**파이프라인 확인 항목**(kj 지시: 중간 저장·재개, 평가 로그, 예산 동일성 I-4):
- **중간 저장**: 평가마다 `<C>.jsonl` 한 줄(지수·값·목적함수·시나리오별 점수·정지사유·\|ω\|·궤적 해시·적분기)과 `<C>.record.json`(진행 수)을 쓴다.
- **재개**: 두 번 확인했다.
  - 예산 12로 끝낸 기록을 24로 이어 돌린 GSLQR·CPID가, 처음부터 24로 돌린 것과 지수·목적함수·115개 시나리오 궤적 해시까지 모두 같았다(서로 다른 프로세스, 병렬 부하 속).
  - 메모리 누수로 종료한 V13(10회)·F13(7회)·M17(1회)을 새 코드로 재개했다. 재생한 지수가 로그와 어긋나지 않았다.
- **I-4**: `--summarize` 결과 `i4_violations = []`다(예산 24 동일, 5종 모두 complete, 시나리오 집합·목적함수·탐색 설정 동일, 튜닝 난수 범위가 본시험과 서로소). 커밋된 기록에 대한 테스트 `test_i4_committed_pilot_tuning_records_are_fair`도 이제 실제로 기록을 검사해 통과한다(이전에는 기록이 없어 skip). 적분기 1% 규칙 걸림은 0건이다.
- **결정성**: 캐시 적중도 예산에 똑같이 계상했다(제어기마다 1회). 재시작 0회. 벽시계 필드는 결과에 없다.

---

## 6. 결정 필요(kj)

### 경기장 정의
1. 논문 §5.6 "60–85 m/s 주시험 제외" 문장과 V_H = 85 m/s(kj 결정)가 충돌한다. 논문 문장을 고칠지.
2. ρ 정의(경로 최대 a_ref/a_avail)와 a_avail(준정상 SLSQP, 역유입 금지 제약). 제동 프로필을 V_H→V_L로 한 것(0까지 내리면 가용 가속도가 불연속), 방향 반전 생략.
3. 대표 돌풍(측풍 10, 수직 −5 m/s)을 전체 8조건으로 넓힐 시점.
4. 실패 판정의 주 기준: 스위트(Acceptance) 대 논문 §5.10. 지금은 둘 다 병기.
5. ~~질량 +30%에서 V_H 트림 존재 여부~~ → 존재한다(회전수 여유 8.44%, 5절). 결정 불필요, 기록만.

### 기준선(CPID·GSLQR)
6. (2.2-c) CPID를 V_H 사례에 넣는 방식. 지금 CPID는 설계 영역(0~20) 밖에서 구조적으로 못 난다.
   논문이 CPID를 핵심 비교군으로 V_H에서도 비교하려면 고속용 CPID가 필요하다(기울기 한계를 트림 자세 기준으로 바꾸고, 트림 스케줄 피드포워드를 넣는 식). 그대로 두면 V_H 비교는 허수아비 비교가 된다.
7. (2.2-e) CPID 세부 구조를 논문 §5.3 서술(자세 루프 + 각속도 루프, 미분 저역통과, 정적 제약 배분)에 정확히 맞출지.
8. (조건부) 같은 예산 튜닝 뒤 CPID 설계 영역 수렴: 24회 최선값으로는 6/6 정착했다(4.1절). 120회 뒤 `--tuned`로 다시 확인. 수렴하지 못하면 이 항목이 살아난다.
9. **적분기 한계 도달 1% 초과 5건**(스모크, 5절 표).
   - CPID 3건은 설계 영역 밖(V_H 출발)이라 6번과 같은 문제다.
   - **GSLQR 2건(질량 ×1.3 10.97%, 모터 τ ×2 4.89%, 둘 다 ξ_vx)**은 새 문제다. 선택지는 셋이다.
     - (가) GSLQR 적분 한계를 CPID처럼 가속도 권한으로 바꾼다. 운용점별 정상상태 사상으로 환산하고, 값은 GSLQR을 약화시키지 않는 쪽으로 정한다.
     - (나) 한계를 넉넉히 키운다.
     - (다) 유지하고, 한계 ∞ 대조로 영향부터 잰다.
   - 권고는 **(다) → 영향이 있으면 (가)**다. 120회 튜닝 전에 정해야 한다(적분기 구조가 바뀌면 튜닝 무효).

### 튜닝
10. **CPID 고정 벌점 문제**(2.2-d, kj 지시로 밤중에 목적함수는 안 바꿨다). 선택지:
    - **(가) 단계적 벌점**: 실패 시행에 `벌점 × (1 − 실패시각/시행길이) + 실패 전까지의 RMSE`를 준다. 오래 버틴 후보가 낫게 나와 실패 구간에서도 기울기가 생긴다. 목적함수 형태는 전 제어기 동일하다. 단점: '늦게 실패'가 '더 나은 제어'와 같지는 않다(천천히 표류하다 실패하는 경우). 전 제어기 튜닝을 다시 해야 한다.
    - **(나) 제어기별 설계 영역 시나리오만**: CPID는 V ≤ 20 m/s 튜닝 시나리오만 쓴다. 신호는 확실해지지만 제어기마다 시나리오 집합이 달라진다('같은 목적함수' 원칙을 '같은 형태의 목적함수'로 완화).
    - **(다) 공통 시나리오에 저속 사례 추가**: 예를 들어 V15 측풍, V20 고도 계단, 0→20 가속을 넣는다. 전 제어기가 같은 집합을 쓰고 CPID 신호가 1개에서 3~4개로 는다. 설계 영역 밖 벌점 상수는 남는다(순위에는 영향 없음).
    - (라) 현행 유지: CPID 튜닝은 V10 한 시나리오로 결정된다는 점을 보고서에 명시한다.
    - 권고: **(다)**. '같은 목적함수'를 지키면서 CPID에 설계 영역 신호를 준다. 필요하면 (가)를 함께 쓴다. 어느 쪽이든 120회 전에 정해야 한다(바꾸면 튜닝 전체 재실행).
11. 120회 본 튜닝 시작 시점(kj 지시로 시작하지 않았다). M17은 평가 1회에 혼자 돌면 ~4분, 다른 작업과 병행하면 ~10분이다(이 컴퓨터). 120회면 M17만 8~20시간이다. 일정 또는 Ubuntu 병행을 정해야 한다.
12. 튜닝된 가중치를 `configs/`에 채택할지(120회 뒤).

### 검증·재현
13. 교차 머신 허용오차(verify 기본 rtol 1e-3, 잠정). Ubuntu에서 한 번 돌려 보정.
14. `verify --quick`은 M17 NLP를 짓지 않는다. 한 번 짓는 데 최대 1.9 GiB라 3 GB 예산에 여유가 없어서다. M17은 전체 모드에서 확인한다. 이 절충을 받아들일지, 다음 세션에서 NMPC 계열 NLP 구성 메모리를 줄일지(세 종 동일 적용·결과 재검증 필요).

### 작업 A 관련
15. 판별 규칙의 '통과' 정의: 80 m/s에서 완주 기준(둘 다 통과)과 추종 기준(둘 다 실패)이 갈린다(3절). 어느 쪽으로 해석할지.
16. (2.2-a) V13·F13 NLP 추력 상한이 정적(식 18)이다. 짧은 시험 포화의 원인 후보라 계획 추력 대 가용 추력 대조를 다음 세션에 할지.
17. 작업 A 보고서의 남은 결정(§2.1 `external/fastdrone_kh`·`models/team_light` 중복 정리, §2.3 증상2, §2.5 85 m/s 여유 9%)은 그대로 유효.

### 스모크에서 새로 생긴 것
18. (2.2-j) M17의 V_H 외란·제동 솔버 실패의 원인 가르기. 다음 세션에서 실패 직전 해의 회전수 제약 활성 여부(H1)를 보고, M17 NLP의 곡선만 C2 스플라인(CasADi bspline)으로 바꾼 진단 실행(H2, 진단 전용)을 권고한다. H2로 판명되면 추진 곡선 표현을 세 NMPC에 똑같이 바꿔야 한다(절대규칙 1). 이 경우 NMPC 결과 전부를 재실행한다.
19. V13 가감속 원래 설정 80 m/s의 감속 중 솔버 정지(3절)와 가감속 실행 중 NaN 경고 1건의 원인 조사 여부.

---

## 7. 변경 파일·커밋·브랜치·테스트

**브랜치**: `protkjj/arena-completion`(push 대상). 기준은 `control`(`cb08eee` 프로펠러 곡선까지 병합)이다.
`control`과 `main`에는 push하지 않았다. 다른 세션이 이 브랜치의 `2976bcb` 위에 튜닝 분석 도구 브랜치
`protkjj/arena-analysis`를 만들었다(병합하지 않음). 튜닝 기록 형식은 필드만 늘었다(`integrators`)라 호환된다.

**변경 파일**(`control` 대비, 역할):

| 파일 | 역할 |
|---|---|
| `configs/arena.json`, `configs/gains/{gslqr,cpid}.json` | 경기장 단일 설정, 기준선 게인(출처·변경 기록 포함) |
| `control/arena.py` | 설정 로드·검증, 확정 사항 대조(I-7), 시나리오 생성, a_avail·ρ, 적분기 1% 규칙 |
| `control/arena_factory.py` | 5종 팩토리, `ReferenceWindow`, `SolverMonitor`(약한 참조), `trim_warm_start`, `IntegratorLog`, 명목 모델(트림·트림 가속도) |
| `control/arena_tune.py` | 같은 예산 튜닝, 재개, I-4 검사, `--summarize` |
| `control/arena_design_check.py` | GSLQR·CPID 설계영역 점검(`--tuned`) |
| `control/arena_integrators.py` | 적분기 한계 점검(권한 환산, 한계 ∞ 대조, CPID 수정 경과, 60초 유지) |
| `control/pilot_before_after.py` | PILOT 전후 비교, 작업 A 판별 재적용 |
| `control/test_arena_fairness.py` | 불변식 I-1~I-11 |
| `control/controller.py` | CPID 적분기(조건부·±1 g·출발값·계측), GSLQR 계측 — 기본값은 기존과 비트 동일 |
| `control/dynamics.py`, `trim.py`, `kh_adapter.py` | hover_quat, x_cp(V), 추진 곡선 헬퍼 |
| `control/nmpc.py`, `nmpc_f13.py`, `hybrid_comparison.py`, `nmpc_common.py` | NMPC 설정 공용화, F13 γ(J)·공력 모멘트 |
| `control/validation_suite.py`, `validation_metrics.py`, `mission_profiles.py` | 스위트 연결, 논문 §5.10 판정, 식(32) 프로필, 적분기 기록, 시행마다 gc |
| `control/kh_repro.py` | 결과에 \|ω\|max 추가 |
| `scripts/verify_arena.py`, `VERIFY.md` | 외부 검증 |
| `results/ARENA_STATUS_2026-09-26.md`, `results/arena/*` | 이 보고서, 설계점검·적분기·PILOT·스모크·튜닝 기록 |

**커밋**(`control` 이후, 오래된 순. 이 보고서·HANDOFF 반영 커밋이 마지막에 하나 더 붙는다):

- `8e82057` 경기장 골격: 설정 파일 하나로 현재 제어기 5종을 민석 스위트에 연결 (S1)
- `4c5389f` Merge branch 'refs/heads/control' into protkjj/arena-completion
- `b049461` CPID 추력→회전수 역산을 팀원 곡선으로 + 공용 추진 헬퍼
- `f42283b` NMPC 계열 설정 공용화(S2) + F13 반토크 γ(J) + F13 어댑터 공력모멘트 누락 수정
- `2976bcb` 경기장: 같은 예산 튜닝 스크립트, 공정성 테스트 I-1~I-9, 튜닝 시나리오 분리
- `b032519` 경기장 관문 1·2: 호버 규약·x_cp(V)·CPID 적분기 기준·적분기 계측 (I-9~I-11)
- `56458e8` PILOT 전후 비교 스크립트: 작업 A 표(선형 프로펠러 시절) 대비 재실행·판별 규칙 재적용
- `9689ab2` 설계영역 점검 결과(PILOT): GSLQR 0~85 10/10, CPID 0~20 6/6 정착, 적분 한계 도달 0%
- `607b008` 적분기 한계 점검 도구: GSLQR 가속도 환산 권한·CPID 트림 필요량·한계 ∞ 비교·CPID 수정 경과·60초 유지 (kj 요청 수치 재현용)
- `871ca66` NLP 메모리 누수 수정: SolverMonitor 순환 참조 제거 + 시행 시작마다 gc
- `cb1841c` PILOT 전후 비교: 마크다운 작성 버그(미실행 부분 참조) 수정, 추종통과 기준 판정 병기, 부분별 git 기록·--merge
- `c11de1d` 적분기 한계 점검 결과(PILOT): GSLQR 10/10·CPID 6/6 한계 ∞와 비트 동일, CPID 수정 경과, 20 m/s 60초 유지(기존 고도적분 +0.175 m)
- `123417e` verify --quick 메모리 예산(3 GB·CPU 1개·20분) 맞춤
- `931897d` 설계영역 점검 --tuned: 튜닝 기록 최선값으로 같은 점검(튜닝 뒤 CPID 설계영역 수렴 확인용)
- `85e3986` PILOT 전후 비교 결과: 작업 A 판별 재적용(85 유지·80은 기준에 따라 갈림), 65초 미션 3/3 완주, 가감속 원래@80만 감속 중 솔버 정지
- `aafd52f` 검증 안내(VERIFY.md)와 스모크 참조(부분): verify 전체·빠른 모드 ALL PASS
- `2a16f90` 스모크 결과(SMOKE): 5종 × 7사례 완주·정지 기록, 전체 참조(35행)로 교체 — 적분기 1% 규칙 걸림 5건(CPID V_H 3, GSLQR 섭동임무 2)
- `1cb7132` 스모크 결과 문서: 셸 heredoc이 백틱을 명령으로 해석해 비었던 재현 명령·git 표기 복구
- `7368a7f` 24회 튜닝 파이프라인 기록(PILOT): 5종 × 24평가 완료, I-4 위반 0, 적분기 1% 규칙 0건 + 최선값 설계점검(CPID 6/6·GSLQR 10/10 정착)

**테스트(최종 코드)**:

| 묶음 | 결과 |
|---|---|
| 공정성 전체 모드(`verify_arena.py`, 6개 프로세스) | 107 passed, 1 xfailed(CPID 85 m/s — 설계 영역 밖, 기대된 실패) |
| 공정성 빠른 모드 | 90 passed, 17 skipped, 1 xfailed |
| 나머지 전체(`control/`, `models/team_light/tests`) | 215 passed, 2 skipped, 1 error(`test_fallback::test_recovery_resets` fixture 오류 — 8/18부터 있던 것, 이번 작업과 무관) |
| `python3 -m control.test_plant`(dynamics.py 변경 시 필수) | ALL TESTS PASSED |
| 기존 동작 비트 동일(CPID·GSLQR 기본값, 무작위 3000상태) | 차이 0.0 |
| `verify_arena.py` 전체 / `--quick` | ALL PASS 11.8분 3.81 GiB / ALL PASS 1.7분 1.23 GiB |

---

## 8. 결정 반영(2026-09-26 낮) — kj 결정 6·9·10·18 + 기울기 45°

kj 결정(2026-09-26 아침):
1. CPID는 저속 실용 기준선으로만 두고 V_H 비교에서 뺀다.
2. 공통 튜닝 집합에 저속 사례를 넣는다.
3. GSLQR 적분 한계는 ∞로 영향부터 보고, 영향이 있으면 넓힌다.
4. M17 솔버 실패는 120회 전에 진단한다. H2·H3로 판명되면 진단 뒤 멈추고 보고한다.
5. 추가: V_L 돌풍은 V_H 대표쌍과 같은 조건으로 한다. CPID 기울기 한계는 PX4 기본값 45°로 한다.

계획서는 `/Users/kj/.claude/plans/rippling-stargazing-karp.md`(승인됨)다.

### 8.1 M17 솔버 실패 진단 — 원인은 추력 0 평탄 구간(H4). **여기서 멈추고 보고한다**

재현: `python -m control.arena_m17_diagnosis`(변형별 새 프로세스). 결과는 `results/arena/M17_DIAGNOSIS.md`, 커밋 `0f56535`다.
진단 전용이라 경기장 설정·제어기는 그대로다.

칸 = 정지 시각 또는 끝까지 · |ω|max(rad/s).

| 변형 | 내용 | 측풍 10 @V_H | 수직풍 −5 @V_H | 제동 V_H→V_L |
|---|---|---|---|---|
| V0 | 기준(참조 궤적 해시 비트 재현) | 3.76 s 정지 · 0.19 | 4.08 s 정지 · 0.11 | 24.2 s 정지 · 0.23 |
| V1 | max_iter 100 | 3.78 s 정지 · 0.22 | 4.16 s 정지 · 0.11 | 23.48 s 정지 · 0.46 |
| V2 | M17 NLP만 C2 곡선(PCHIP 대비 ≤1.93%, 플랜트 불변 확인) | 3.76 s 정지 · 0.19 | 4.22 s 정지 · 0.11 | 23.26 s 정지 · 0.03 |
| V3 | 입력 편차 기준 = 트림 회전수 | 3.76 s 정지 · 0.19 | 4.16 s 정지 · 0.11 | 22.66 s 정지 · 0.22 |
| **V4**(사후) | 명령 하한 = 실측 축방향 속도의 양추력 회전수 | **12 s 끝까지** · 0.95(실패 4, 연속 1) | **12 s 끝까지** · 0.16(실패 0) | **52.1 s 끝까지** · 0.07(실패 0) |

- 멈춘 실행의 |ω|는 작다. 멈춘 원인이 자세 발산이 아니라 솔버 연속 실패이기 때문이다.
- 멈춘 실행과 끝까지 간 실행의 |ω|max는 비교할 수 없다. 측풍 V0는 돌풍(3~5 s) 도중에 멈췄고, V4는 돌풍 전체를 겪었다.

- **미리 정한 판정**: H1(한계 활성)·H2(C1 곡선)·H3(호버 기준 편차)·반복 부족 모두 세 사례에서 **불지지**다.
- **공통 관찰**: V0~V3 모든 사례의 실패 풀이에서 계획 명령이 **100% 추력 0 평탄 구간**(J > 양추력 한계 1.3485)에 들어갔다. 성공 풀이에서는 0.5~28%였다. V4에 남은 실패 4건은 평탄 구간과 무관하다(0/4 — 하한이 평탄 구간을 막는다).
  - 85 m/s에서 트림 회전수(0.86~0.91·n_max)와 추력 0 회전수(~0.81·n_max)의 차이는 5~10%다. 모멘트를 만들려고 로터 하나의 추력을 줄이는 계획이 평탄 구간(추력 0에 묶임, 기울기 0, `fmax(Ct,0)` 꺾임)에 쉽게 들어간다.
  - 거기서 뉴턴 반복이 막힌다. V2가 내부 매듭만 매끄럽게 해서는 안 풀린 이유다.
- **사후 변형 V4**(판정 규칙 "V0가 멈춘 사례를 V4가 끝까지 가면 H4 지지"를 실행 전에 코드·문서에 적음): 세 사례 모두 끝까지 갔다 → **H4 지지**.
- **왜 M17뿐인가**: V13·F13은 NLP 입력이 추력이라 "추력 ≥ 0"이 깔끔한 상자 제약이다. M17은 회전수 공간에서 계획해서, 같은 제약이 기울기 0인 평탄 구간이 된다.

**수정 선택지(kj 결정 필요 — 결정 전에는 적용하지 않음)**:
- (가) **M17 명령 하한을 실측 축방향 속도의 양추력 회전수로**(V4 그대로). 팀 코드 `propeller_curve.supported_rate_bounds`의 하한과 같은 개념이다.
  - V13·F13의 "추력 ≥ 0"을 회전수 공간에 옮긴 것이라, 세 NMPC의 실현 가능 집합이 같아진다(↑M17, 기울기 해소).
  - 한계 1: 하한을 매 풀이의 실측 대기속도로 고정한다(예측 구간 안의 속도 변화는 반영하지 않음).
  - 한계 2: 실패를 전부 없애지는 않는다. V4 측풍에서 실패 풀이 4건(연속 최대 1)이 남았고, 4건 모두 명령 한계가 활성이었다. 정지 기준(연속 5)에는 걸리지 않는다.
  - 적용하면 M17 스모크를 전부 다시 돌려야 한다. 24회 파이프라인의 M17 기록은 옛 튜닝 집합이라 어차피 superseded다(8.3).
- (나) 제어기 모델 곡선의 추력 0 경계를 매끄럽게 하거나 음추력(풍차)으로 연장한다. 제어기 모델 전체(V13·F13·GSLQR·CPID 경로)가 바뀌어 범위가 크다.
- (다) 그대로 두고 M17의 한계(회전수 입력 + 평탄 구간)로 보고한다.
- **권고: (가)**. 범위가 가장 작고, 세 NMPC가 같은 제약을 받게 되며, 근거 개념이 팀 코드에 이미 있다.

### 8.2 GSLQR 적분 한계 — 영향 있음 → 5에서 50으로 넓힘

재현: `python -m control.arena_integrators --smoke-cases --limits 1e9 50`. 결과는 `results/arena/INTEGRATORS_SMOKE.md`, 커밋 `40ab0da`·`7addbab`이다.
- **질량 ×1.3**: 미리 정한 규칙상 '영향 있음'이다(정지 44.33 → 44.59 s).
  - 궤적은 37.94 s까지 비트 동일하다.
  - 두 실행이 함께 살아 있던 구간에서는 ∞가 RMSE v 7.96 → 7.11, z 5.17 → 4.89로 조금 낫다. 창 RMSE z가 18% 나빠 보인 것은 평가 구간 길이 차이의 착시였다.
- **모터 τ×2**: 영향 없음. 두 사례 모두 결말(감속 중 발산)은 같다.
- **|ω|max**(rad/s): 질량 ×1.3은 26으로 두 한계가 같다. 모터 τ×2는 33.1 → 34.6이다(넓힌 쪽이 조금 크다). 두 사례 모두 발산 정지 직전의 값이다.
- **조치**: kj 규칙대로 넓혔다. 50은 후보(5→50→500) 중 GSLQR 스모크 7사례 **전부 한계 도달 0%**가 되는 가장 작은 값이다(ξ_vx 최대 5.99).
  - 설계점검 10건은 비트 동일하다(한계에 안 닿음).
  - **결과를 본 뒤·예산 밖·GSLQR 유리(기준선 강화)**로 기록한다(`configs/gains/gslqr.json` change_record).

### 8.3 공통 튜닝 집합에 저속 사례 3개 추가(5 → 8개)

재현: `python -m control.arena_tuning_candidates --controllers V13 M17 F13 GSLQR`(~11분, 최대 RSS 2.75 GiB). 결과는 `results/arena/TUNING_CANDIDATES.md`다.
- 추가: `tune_gust_lateral_p5_V15`, `tune_gust_vertical_p3_V20`, `tune_ramp_0_15_rho0.1`.
  - 미리 정한 선택 규칙대로, 사전 게인 CPID(45°)가 실패 없이 끝낸 첫 단계 후보를 그대로 채택했다(2단계 후보는 돌릴 필요가 없었다).
  - 도구가 고른 사례와 `configs/arena.json`의 명세가 같은지 대조한다(세 사례 모두 같음).
- **5종 모두 사전 게인으로 세 사례를 실패 없이 끝냈다.** 새 사례에서 고정 벌점을 받는 제어기가 없다는 뜻이다. 적분 한계 도달은 CPID·GSLQR 모두 0%다.

| 사례 | CPID | V13 | M17 | F13 | GSLQR |
|---|---|---|---|---|---|
| 측풍 5 @15 | 0.168 / 0.039 · 0.12 | 0.138 / 0.011 · 0.22 | 0.115 / 0.010 · 0.23 | 0.137 / 0.013 · 0.22 | 0.088 / 0.009 · 0.32 |
| 수직풍 3 @20 | 0.070 / 0.065 · 0.09 | 0.082 / 0.019 · 0.10 | 0.053 / 0.015 · 0.14 | 0.078 / 0.025 · 0.10 | 0.036 / 0.019 · 0.20 |
| 가속 0→15 ρ 0.1 | **2.716** / 0.182 · 0.33 | 0.035 / 0.005 · 0.43 | 0.072 / 0.012 · 0.43 | 0.035 / 0.007 · 0.43 | 0.581 / 0.135 · 0.53 |

칸 = 창 RMSE v / z(m/s, m) · |ω|max(rad/s). 사전 게인, 튜닝 전 값이다(PILOT, 우위 결론 없음).

- **가속 사례의 CPID 2.72는 구조 탓으로 보인다(영향 확인 필요 → 8.7).**
  - 경기장 CPID는 참조 가속도 피드포워드가 없다. 매 스텝 현재 참조 속도만 받는다(`arena_factory.update_at`).
  - PX4 `PositionControl`은 궤적 설정점의 가속도를 `_acc_sp`로 받고, 그 위에 속도 PID 출력을 더한다(`setInputSetpoint`, `_velocityControl`의 `addIfNotNanVector3f(_acc_sp, acc_sp_velocity)` — PX4 main `7c8d6daca9be`에서 확인).
  - 1차 어림(수평 PI 속도 루프만, Kp 1·Ki 0.15, 이 사례의 실제 참조, 최대 참조 가속 6.71 m/s²): 피드포워드가 없으면 RMSE v 2.40(최대 지연 4.7 m/s), 있으면 ~0이다. 실측 2.72의 대부분이 이 차이로 설명된다.
  - GSLQR(0.58)도 참조 가속도를 쓰지 않는다. NMPC 세 종은 1 s 미리보기로 참조 변화를 안다.
  - 본시험에서 CPID가 비교되는 사례(V_L 돌풍 두 개)는 참조가 일정해 이 차이의 영향이 없다. 튜닝에서는 CPID가 이 지연을 Kp_vel로만 줄여야 해서, 게인이 가속 사례 쪽으로 끌린다.
- **평가 창**: 경기장의 참조 사례 창은 논문 창 [램프 시작 −1 s, 램프 끝 +5 s]다. 튜닝 가속 사례는 lead 0.5 s·tail 1 s로 짧아서 창이 기록보다 길고, 결과적으로 기록 전체(0~7 s)로 평가된다. 옛 튜닝 가속 사례(40→70)도 같고, 전 제어기에 똑같이 적용된다.
- 설계 영역 밖 4개의 벌점 상수는 8개 평균에서 500이고, 순위에는 영향이 없다.
- **재개 가드**: 튜닝 기록의 설정 해시·시나리오 id가 지금과 다르면 멈춘다. 옛 목적함수를 조용히 재생하던 위험을 없앴다.
- `results/arena/tuning/pilot24`는 **옛 집합(5개) 기준 파이프라인 검증**으로만 남는다(superseded). 새 집합의 24회·120회는 새 run-dir로 돌린다.

### 8.4 CPID 범위·기울기 45°·V_L 본시험

- **CPID는 설계 영역(0~20 m/s) 밖 본시험에서 제외한다.** 스모크 결과에 `skipped` 행(사유 포함)으로 남기고 시뮬레이션하지 않는다. 제외 쌍을 이름으로 요청하면 오류로 막는다(`control/arena.py::excluded_from`).
- **V_L 돌풍 본시험 추가**: `gust_lateral_p10_VL`, `gust_vertical_m5_VL`. V_H 대표쌍과 같은 조건이라 CPID에 맞춰 고른 조건이 아니다(I-7 테스트가 묶어 둠). 측풍 10 m/s는 V_L에서 유속각 26.6°로 5종 모두에게 강하다.
  - CPID가 비교되는 본시험은 이 둘이다. 본시험은 9개가 됐다.
- **CPID 기울기 한계 35° → 45°**: PX4 기본값 `MPC_TILTMAX_AIR`이다(PX4 main `multicopter_position_control_limits_params.yaml`, default 45.0 확인).
  - 원고가 CPID를 PX4 구조로 서술하고, 20 m/s 트림(33.4°)이 35°의 가장자리였다.
  - **결과를 본 뒤의 구조 정렬·CPID 유리**로 기록한다. 클래스 기본 35°는 그대로다.
  - 설계점검은 6/6 비트 동일하다(설계점검에서는 기울기 한계에 안 닿았다).
- **±1 g 수평 적분 한계 주석**: PX4 `PositionControl`은 **수직** 속도 적분만 ±1 g로 자르고, 수평은 고정 한계 없이 추적형 안티와인드업을 쓴다. 경기장의 수평 ±1 g는 PX4 수직 기준을 수평까지 넓힌 것이다. 설계 영역 안에서는 닿지 않는다(한계 ∞와 비트 동일).

### 8.5 현재 상태와 다음

- **로컬 커밋만 있고 push하지 않았다.** `configs/arena.json`이 바뀌어(V_L 사례·튜닝 집합) 스모크 참조를 다시 만들기 전까지 `verify_arena.py` 4단계가 설정 해시 불일치로 실패하기 때문이다.
  - 원격(`d1c1cdb`)은 일관된 이전 상태다.
  - 로컬 커밋: `2bf67e0`(M17 진단 도구), `40ab0da`(GSLQR 한계 대조), `7addbab`(GSLQR 한계 50), `0f56535`(M17 진단 결과), `e72cff7`(CPID 범위·45°·V_L·튜닝 집합·재개 가드), 그리고 이 보고서 커밋.
- 이번 반영에서 바꾼 파일:
  - 설정: `configs/arena.json`(V_L 돌풍 2개, 튜닝 저속 3개), `configs/gains/cpid.json`(45°), `configs/gains/gslqr.json`(한계 50)
  - 코드: `control/arena.py`(`excluded_from`), `control/validation_suite.py`(제외 행), `control/arena_tune.py`(재개 가드), `control/arena_integrators.py`(`--smoke-cases`)
  - 새 도구: `control/arena_m17_diagnosis.py`, `control/arena_tuning_candidates.py`
  - 테스트·검증: `control/test_arena_fairness.py`(새 테스트 4개), `scripts/verify_arena.py`, `VERIFY.md`
  - 결과: `results/arena/{M17_DIAGNOSIS,INTEGRATORS_SMOKE,TUNING_CANDIDATES}.md`(+ json)
- **스모크 재생성은 8.1·8.7 결정 뒤로 미뤘다**(kj 선택: 두 번 돌리지 않게). 8.1 (가)를 적용하면 M17 결과가, 8.7 (가)를 적용하면 기준선 결과가 바뀔 수 있기 때문이다.
- 테스트는 8.6 참고.
- 논문 원고 수정이 필요한 곳(kj 원고, 코드 밖):
  - 표 5에서 CPID를 '저속 실용 기준선'으로 바꾸고 GSLQR 역할 서술을 고친다.
  - §5.3 304행 '핵심 비교군 4개'.
  - §5.9 421행 통합임무 비교 구성의 CPID.
  - 표 11·12의 CPID 행.
  - §5.6 대표 돌풍에 V_L 쌍을 넣는다.

### 8.6 이번 반영의 테스트

| 묶음 | 결과 |
|---|---|
| 공정성 전체 모드(묶음별 6개 프로세스) | 111 passed, 1 xfailed(CPID 85 m/s — 기존의 기대된 실패) · 644초 · 최대 3.77 GiB |
| 공정성 빠른 모드(새 프로세스에서 따로 잼) | 94 passed, 17 skipped, 1 xfailed · 130초 · 최대 1.23 GiB(3 GB 예산 안, 전과 같음) |
| 새 테스트 5개(두 모드 모두 통과) | I-7 CPID 저속 기준선(V_L 쌍 = V_H 쌍, id·속도만 다름) · I-7 제외 쌍 명시 요청 거부 · I-7 제외 쌍이 스모크 루프에서 '제외' 행으로 참조·보고서까지 남음(시뮬레이션은 가짜로 바꿈) · I-11 경기장 CPID 45°·클래스 기본 35° · I-4 재개 가드(설정 해시·시나리오 불일치 → 오류) |
| 제외 행 테스트(5번째)의 확인 | 위 두 모드 실행 뒤에 추가했다. 그래서 그 묶음(rest)만 두 모드로 다시 돌렸다: 65 passed / 65 passed. 제외 판정을 끈 변형(뮤턴트)에서는 이 테스트가 실패한다(검출력 확인) |
| 나머지 전체(`control/`, `models/team_light/tests`) | 215 passed, 2 skipped, 1 error(기존 `test_fallback::test_recovery_resets` fixture 오류, 이번 작업과 무관) |
| 설계점검 비트 동일(적용 전후) | CPID 6/6(기울기 35° → 45°)·GSLQR 10/10(적분 한계 5 → 50) — 두 변경 모두 설계점검에서는 한계에 닿지 않았다 |
| `python3 -m control.test_plant` | 해당 없음(`control/dynamics.py` 불변) |
| `verify_arena.py` 전체·`--quick` | **돌리지 않았다.** 스모크 참조를 다시 만들기 전이라 4단계(설정 해시)가 실패하는 것이 정상이다. 결정 뒤 스모크를 다시 만들 때 함께 돌린다 |

- 테스트 수는 108 → 113이다(새 테스트 5개). 위 두 모드 실행은 112개 시점이다.
- 시간은 전보다 길다(전체 458 → 644초, 빠른 모드 69 → 130초).
  - 새 테스트가 ~21초다(5번째 제외 행 테스트 3초는 별도). I-7 저속 기준선 테스트가 시나리오 9개를 만드느라 16.5초를 쓴다.
  - 나머지 증가분은 측정 당시 다른 앱이 CPU를 쓰던 탓으로 보인다. 따로 재지는 않았다. 20분 예산과는 거리가 멀다.
- 전체 모드 도중 컴퓨터가 잠자기에 들어갔다가 깼다. 결과(통과 수)에는 영향이 없다. 시간은 잠든 구간을 뺀 값이다(`time.perf_counter`).

### 8.7 새 결정 필요(2026-09-26 낮)

1. **M17 추력 0 평탄 구간 수정(8.1)**
   - (가) M17 명령 하한을 양추력 회전수로 둔다. **권고**다.
   - (나) 제어기 모델 곡선의 추력 0 경계를 매끄럽게 하거나 음추력으로 연장한다.
   - (다) 그대로 두고 M17의 한계로 보고한다.
   - 적용하면 M17 스모크를 전부 다시 돌린다. 24회 PILOT의 M17 기록은 옛 집합이라 이미 superseded다.
2. **기준선의 참조 가속도 피드포워드(8.3, 2.2-k)** — 이번에 새로 발견했다.
   - (가) CPID에 참조 가속도 피드포워드를 넣는다(PX4 `_acc_sp`와 같은 구조). 창에서 참조 가속도를 읽어 가속도 명령에 더한다.
     - PX4 구조 정렬이고 기준선 강화다. **결과를 본 뒤·CPID 유리**로 기록한다.
     - 참조가 일정한 사례(V_L 돌풍 본시험)에서는 더하는 값이 0이라 결과가 비트 동일할 것으로 예상한다(적용 후 확인 필요).
   - (나) 그대로 두고 기준선의 구조 한계로 보고한다. 튜닝 가속 사례는 유지한다. CPID 게인이 가속 사례 쪽으로 끌리는 것을 감수한다.
   - (다) 그대로 두고 가속 사례를 튜닝 집합에서 뺀다(저속 돌풍 두 개만 남긴다).
   - **권고: CPID는 (가).** 지금까지의 CPID 정렬(기울기 45°, 적분 ±1 g)과 근거가 같다(원고가 CPID를 PX4 구조로 서술). "모호하면 기준선을 강하게"라는 규칙에도 맞는다.
   - GSLQR에도 같은 원칙을 적용할지는 kj 판단이 필요하다. PX4 같은 원본 구조가 없어 LQI의 참조 피드포워드를 새로 설계해야 한다. 넣으면 V_H 참조 변화 사례(제동·가감속)의 GSLQR 결과가 바뀐다.
3. **결정 뒤 순서**: 1·2 적용 → 스모크 전체 재생성(9사례, CPID는 V_L 두 사례만, ~3시간) → 참조 교체 → `verify_arena.py` 전체·`--quick` → 보고서 갱신 → push.
4. **120회 본 튜닝**은 그다음 kj 지시로 시작한다(새 집합 8개, 새 run-dir).

## 부록 A. 백그라운드 실행 — 시작 명령과 진행 확인

모든 긴 실행은 스레드 1개로 고정한다. 결과는 부하·병렬 실행과 무관하다(재개 검증, 두 스모크 실행의 10/10 비트 일치).

### A.1 24회 파이프라인(완료, PILOT)

```bash
for C in V13 M17 F13 GSLQR CPID; do
  caffeinate -dims env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 VECLIB_MAXIMUM_THREADS=1 \
    python3 -m control.arena_tune --controllers $C --budget 24 --run-dir results/arena/tuning/pilot24 \
    > results/arena/tuning/pilot24_$C.log 2>&1 &
done
python -m control.arena_tune --summarize --run-dir results/arena/tuning/pilot24   # I-4 + 적분기 1% 규칙
```

### A.2 120회 본 튜닝(**시작하지 않았다** — kj 지시. 시작 전에 결정 필요 10을 정할 것)

```bash
mkdir -p results/arena/tuning/main120
for C in V13 M17 F13 GSLQR CPID; do
  caffeinate -dims env OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 VECLIB_MAXIMUM_THREADS=1 \
    python3 -m control.arena_tune --controllers $C --budget 120 --run-dir results/arena/tuning/main120 \
    > results/arena/tuning/main120_$C.log 2>&1 &
done
```

- **진행 확인**:
  - `tail -1 results/arena/tuning/main120_M17.log`는 `[M17] eval k/120 objective=…`를 보여 준다.
  - 기록 파일은 `python3 -c "import json; r=json.load(open('results/arena/tuning/main120/M17.record.json')); print(r['status'], r['spent'], r['budget'])"`로 본다.
  - 한꺼번에 보려면 `python -m control.arena_tune --summarize --run-dir results/arena/tuning/main120`을 쓴다(I-4 위반과 적분기 1% 규칙 기록 포함).
- **메모리 확인**: `top -l 1 -o mem -stats pid,command,mem,cmprs`. `ps` RSS는 압축 메모리를 못 본다. 누수 수정 뒤 실측은 프로세스당 0.6~2.5 GB다.
- **중단·재개**: 같은 명령을 다시 실행하면 `<C>.jsonl`에서 끝난 평가를 그대로 재생하고 이어 간다. 재생한 지수가 로그와 다르면 멈춘다. 코드나 설정이 바뀐 경우이므로 새 run-dir로 시작할 것.
- **예상 시간**(이 컴퓨터, 다른 작업과 병행):
  - 평가 1회: M17 4분(혼자)~10분(병행), F13 2~3분, V13 1.5~2분, GSLQR·CPID 10초 안팎.
  - 120회 전체: **M17 8~20시간**, F13 4~6시간, V13 3~4시간.
- **끝난 뒤**: `python -m control.arena_design_check --tuned results/arena/tuning/main120 --out results/arena/design_check_main120`로 CPID 설계 영역 수렴을 다시 확인한다(kj 규칙). 그다음 기록을 커밋하면 I-4 커밋 기록 검사(`test_i4_committed_pilot_tuning_records_are_fair`)가 돈다.

### A.3 스모크·PILOT·점검

| 실행 | 명령 | 시간(이 컴퓨터) |
|---|---|---|
| 스모크 전체 | `python -m control.validation_suite --config configs/arena.json --smoke --reference-out results/arena/smoke_reference.json` | ~3시간 |
| PILOT 전후 | `python -m control.pilot_before_after` | ~1시간 |
| 설계점검 | `python -m control.arena_design_check [--tuned <run-dir>]` | ~3분 |
| 적분기 점검 | `python -m control.arena_integrators --binding --history --hold` | ~10분 |
| GSLQR 적분 한계 스모크 대조(8.2) | `python -m control.arena_integrators --smoke-cases --limits 1e9 50` | ~1.5분 |
| M17 솔버 실패 진단(8.1) | `python -m control.arena_m17_diagnosis`(변형 5개, 기본 `--parallel 2`, 프로세스당 최대 ~1.9 GiB) | ~1시간 |
| 튜닝 저속 사례 고르기(8.3) | `python -m control.arena_tuning_candidates --controllers V13 M17 F13 GSLQR` | ~11분(최대 RSS 2.75 GiB) |
| 검증 | `python scripts/verify_arena.py [--quick]` | 11.8분 / 2.1분 |

### A.4 이번 밤의 사고와 조치(kj 부재 중, 사후 보고)

1. **메모리 누수 → 백그라운드 실행 종료·재개(01:02)**:
   - 증상: 24회 튜닝과 스모크를 병렬로 돌리자 프로세스가 평가 1~2회 만에 M17 10 GB, V13·F13 6 GB로 불었다(압축 메모리 포함). 스왑은 11.8/12.3 GB였다.
   - 원인: `SolverMonitor`의 순환 참조(`871ca66`에서 수정).
   - 확인: 새 코드가 튜닝 로그의 궤적 해시와 4/4 비트 동일했다.
   - 조치: 네 프로세스(V13·M17·F13 튜닝, 스모크)를 종료하고 새 코드로 재개했다.
   - 손실: 튜닝은 끝난 평가를 JSONL에서 재생해 진행 중이던 1회분만 잃었다. 스모크는 처음부터 다시 돌렸다. 재개 뒤 메모리는 프로세스당 0.6~2.5 GB였다.
   - kj 규칙상 프로세스 종료는 확인받을 일이지만, "질문하지 말고 마무리" 지시와 시스템 보호를 우선했다.
2. **`verify --quick` 3 GB 예산**:
   - 한 프로세스에서 4.4 GiB까지 갔다.
   - 조치 1: 묶음별 새 프로세스로 돌린다.
   - 조치 2: 짧은 시행 픽스처가 NLP 대신 스냅샷만 보관한다.
   - 조치 3: 빠른 모드는 M17 NLP를 짓지 않는다.
   - 결과는 1.23 GiB다(`123417e`).
3. **PILOT 스크립트 버그**: 첫 실행이 마크다운 작성에서 멈췄다(아직 안 돈 부분 참조). 고친 뒤(`cb1841c`) 판별까지 다시 돌렸다. 두 실행의 판별 수치는 같았다.
