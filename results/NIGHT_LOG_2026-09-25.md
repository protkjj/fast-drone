# 야간 작업 로그 2026-09-25

시작: 01:29. 기준 테스트: **90 passed, 1 error**(선행 error, `test_fallback.py::test_recovery_resets`
픽스처 오해 — 이전 세션들에서 이미 확인됨, 이번 작업과 무관).
브랜치: `control`, worktree `/Users/kj/Desktop/dynamic/fast_drone-control-paper`.
최근 커밋 `593b017`(프로펠러/항력 모델 수정 + 고속 트림가지 발견).

시작 전 읽기: MEMORY.md·VL_VH_DERIVATION.md·HIGH_SPEED_TRIM_BRANCH.md·PAPER_V53_ALIGNMENT.md
는 이번 세션에서 직접 작성/갱신한 것이라 내용을 이미 보유. `research/TRIM_CAUSE_AND_LEVERS.md`도
이번 세션에서 정독함. 이번에 새로 읽은 것: `research/DESIGN_SOURCE_AUDIT.md` §4 — **40A는 제품
정격이 아니라 웹 연구 모델이 추가한 가정**(원본 CSV에는 없음; 배터리 40C 연속방전율에서 역산한
균등 배분 39.949A/모터). 실제 모터/ESC 제품 정격은 자료로 확정 불가.

---

## 단계 1. 85 m/s 트림 전류 진단

**결과 요약**: 계획된 45분 예산을 크게 초과(약 2시간) — 진단 도중 저녁에 보고한
"고속가지" 수치 자체가 틀렸음을 발견해(J_max 재적합 이후 트림점이 표 유효범위를
벗어남) 먼저 바로잡아야 했다. `control/trim_hifi.py`(표 기반 직접 트림 솔버) 신규
작성, 저속가지 하이파이 경계(19.626)가 research 독립값(19.63)과 일치해 솔버
신뢰성 확보. 고속가지 재개방 = 81.14(67.99 아님), 85m/s 요구전류 44.83A(+12.1%,
research 45.61A/+14.2%와 근접) — **research의 전류 제약 결론을 뒷받침**, 반박하지
않음. 커밋 `bb14f90`. 상세는 HIGH_SPEED_TRIM_BRANCH.md 정정판 참조.

시간 초과 사유 기록(규칙9): 이 정정 없이 다음 단계(2·4·5)를 진행하면 전부 틀린
트림가지 위에서 진행하는 것이라 시간 상한보다 정합성을 우선했다. 남은 단계들은
이 지연을 반영해 범위를 줄여 진행한다.

---

## 단계 2. 고속 가지 섭동 강건성과 V_L·V_H 후보 (범위 축소, ~40분)

**결과**(원시출력 `results/night_perturbation_output.txt`, 재현:
`python3 -m control.night_perturbation_robustness`, 정밀도 ±0.5 m/s — 표8
수준 아님):

- 🔴 **질량 +30%, 무게중심 -10%팔 섭동에서 고속가지(85 m/s)가 아예 사라진다.**
  고속가지는 명목·나머지 섭동(추력계수 ±30%, 동체공력 +50/+100%, 공력감쇠
  ±50%)에서는 78~83 m/s 범위에서 안정적으로 재개방하지만, 이 두 섭동에는
  취약하다 — VH 후보로 쓰려면 이 두 조건을 반드시 표8에 포함해야 한다.
- 저속가지는 기존 VL_VH_DERIVATION.md(단순화 모델)와 같은 경향: 동체공력
  +100%가 가장 나쁘다. 다만 이번 스캔 구간(15~22)이 좁아 그 섭동의 정확한
  상한은 못 잡았다(경계가 15 미만일 수 있음 — 저녁 분석의 15.304와 일치할
  가능성 높음, 재확인 필요).
- 모멘트 여유(정적 근사, 신뢰도 낮음): 85 m/s 명목에서 ≈0.49 N·m. 측풍
  환산은 유속각만 계산(5→3.4°, 10→6.7°, 15→10.0°) — §4.7 프레임의 rν/εν
  분해는 시간상 못 함, "판정 불가"로 남긴다.

**시간 상한 처리(규칙9)**: 정밀 이분탐색·전체 §4.7 프레임 여유 계산은 생략.
저속가지 동체공력+100% 정확한 경계 재확인도 생략 — 다음 세션 후보로 이월.

---

## 단계 3-a. M17·F13 비용함수 논문식 통일 (최우선, ~50분)

**결과**: 완료, 목표시간(50분)보다 다소 오래 걸렸으나 상한 크게 안 넘음.
M17·F13에 `cost_spec='paper'` 추가, 기본값을 V13과 맞춰 'paper'로 전환.
검증: 비용함수 손계산 대조(M17 상대오차 0, F13 1.63e-16 — 기계정밀도),
결정변수 수가 논문 식(31)과 정확히 일치(437/353), 양쪽 축 호버 트림 일치
(1e-3 이내). 신규 테스트 10개(`test_cost_spec_m17_f13.py`) + 가드 테스트
갱신. 전체 107 passed(+10), 선행 error 1건 동일.

**남은 비대칭**: acados 경로(`AcadosVirtualNMPC`)는 여전히 legacy(별도
클래스). `bench_compute.py` 등 4개 스크립트가 cost_spec 없이 호출하므로
재실행하면 결과가 바뀐다(V13 때와 같은 패턴). PAPER_V53_ALIGNMENT.md 갱신.

---

## 시간 재배분 결정 (규칙9)

단계1 정정에 예상(45분)의 약 3배가 들었다. 남은 시간에 전부를 원래 상세도로
못 끝낸다고 판단해 우선순위를 다시 매긴다.

- **3-b(튜닝 예산 스크립트), 3-c(난수 격리 테스트): 이번엔 생략.** 요구사항과
  설계만 최종 보고서에 남기고 구현은 다음 세션으로 넘긴다 — 실제 튜닝 리허설을
  졸속으로 돌리면 §5.3(본시험 비참조 등)을 잘못 지킬 위험이 시간 압박보다 크다.
- 3-d(acados 정리), 3-e(CPID, 25분 상한 엄수)는 계획대로 시도한다.
- 단계4(GSLQR 간극 설계문서)는 실행보다 설계 비교라 시간 대비 가치가 높아 그대로 간다.
- 단계5(간극 관통 파일럿)는 축소해서라도 시도한다 — 가설 확인 자체가 보고서의
  핵심 근거 중 하나다고 판단했다.
- 최종 보고서 작성 시간은 반드시 확보한다.

---

## 단계 3-d. acados 경로 정리 (~15분)

**결과**: control/vnmpc_acados.py 모듈 독스트링에 "이 경로는 계산 시간 측정
전용, 표5·6·8 결과는 항상 IPOPT" 명시. `self.is_timing_only = True` 속성
추가(프로그램적 확인용). 가드 확인: `control/fair_compare.py`가 애초에
acados를 등록하지 않아 공정 비교 하네스로 섞일 경로가 없음을 재확인.
구문 검사 통과, acados_template import 확인(단 DYLD_LIBRARY_PATH 없이는
실제 솔버 빌드는 안 됨 — 런타임 스모크 테스트는 시간상 생략).

---

## 단계 3-e. CPID x축 추력 지원 (25분 상한)

**결과**(25분 상한 준수): `_force_to_attitude`를 축 분기로 재작성 —
z축은 기존과 **비트 동일**(300개 무작위 상태 최대차이 0.0), x축은 **발산이
멈췄다**(과거 8→66.5 m/s 폭주 → 이번엔 V=8: 6.96, V=15: 11.60로 수렴은
하되 목표보다 13~23% 낮게 정착). 시간 상한으로 이 잔차의 원인은 못 팠다 —
`GeometricGuidance`와 달리 CPID엔 적분 게인이 고도에만 있고 속도엔 없어서일
가능성이 있으나 미확인. 테스트 4개(z축 회귀, x축 비발산, R_des 직교성)로
"발산 재발 방지"만 고정 — "정확한 트림"은 다음 세션 몫으로 열어 뒀다.
